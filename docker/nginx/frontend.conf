server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index auth/login.html;

    location = / {
        return 302 /auth/login.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
